import { useEffect, useState } from "react"

export const Controls = () => {
    

    return (
        <div className={`controls ${visible ? '' : 'hidden'}`}>
            potato
        </div>
    )

}